name: Build Huda Android production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_android:
    name: Build Android AAB
    type: build
    params:
      platform: android
      profile: production